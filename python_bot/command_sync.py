# TODO extract syncing of commands out of client setup, so it does not get called everytime the bot is started
# commands only need to be synced when command parameters have changed


if __name__ == '__main__':
    pass
